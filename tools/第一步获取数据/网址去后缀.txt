javascript:(function(){let u=new URL(window.location.href);let p=u.searchParams;let dirty=['utm_source','utm_medium','utm_campaign','utm_term','utm_content','spm','spm_id_from','vd_source','share_source','share_medium'];dirty.forEach(k=>p.delete(k));let clean=u.toString();window.history.replaceState({},document.title,clean);navigator.clipboard.writeText(clean).then(()=>alert('✅ 网址已净化并拷入剪贴板：\n'+clean));})();
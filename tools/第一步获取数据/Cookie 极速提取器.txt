javascript:(function(){let c=document.cookie;if(!c){alert('❌ 当前网站无 Cookie！');return;}let f=c.split('; ').join('\n');navigator.clipboard.writeText(f).then(()=>alert('✅ 通行证 (Cookie) 已提取并复制到剪贴板！\n可直接粘贴用于 Python/Curl 爬虫授权。'));})();